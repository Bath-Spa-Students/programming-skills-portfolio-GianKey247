# Define a function called make_shirt with two parameters: size and message.
# Display 2 messages, they have different size but same message.
def make_shirt(size, message="I love Python"):
    # Print the size and message of the shirt.
    print(f'shirt size is {size} and its message is {message}')
make_shirt("large")
make_shirt("medium")